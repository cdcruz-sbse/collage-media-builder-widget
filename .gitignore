# Dependencies
node_modules/

# Build output. The bundle is intentionally committed so it can be served
# from a CDN (jsDelivr) for hosting the widget; rebuild it with `npm run build`.
dist/*
!dist/chriscelle.collage-media-builder.js

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
